#!/usr/bin/python
#~ import pyexiv2
#~ import pickle

import kaa.metadata
import time
#~ exif_tags = []
#~ metadata = pyexiv2.metadata.ImageMetadata('/media/EOS_DIGITAL_/DCIM/100EOS5D/IMG_1798.ARW')
#~ metadata.read()
#~ for tag in metadata.exif_keys:
    #~ print tag
    #~ exif = metadata[tag]
    #~ s = pickle.dumps(exif)
    #~ exif2 = pickle.loads(s)
    #~ exif_tags.append(exif)
    #~ print exif2.raw_value

#~ for tag in exif2.exif_keys:
    
    #~ exif_tags.append()

time.sleep(20)
