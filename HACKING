if gobject.pygtk_version < (2,8,0):

install:

hal-doc
Python bindings for the GNOME desktop environment - documentation
Python bindings for the GNOME desktop environment - documentation

in update of progress bar:

from gnome app:

flushevents(self)

        while gtk.events_pending():
            gtk.main_iteration()



gnomevfs.format_file_size_for_display() aha!!!
gnomevfs.get_local_path_from_uri

gnomevfs.VolumeMonitor.get_volume_for_path
